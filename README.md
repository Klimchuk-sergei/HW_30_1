## 🚀 Основные функции

### ✅ Реализовано в текущей версии:
- **JWT-авторизация** с кастомной моделью пользователя (email вместо username)
- **CRUD операции** для пользователей, курсов и уроков
- **Система прав доступа** с ролями: обычный пользователь, модератор, администратор
- **Автоматическая привязка владельца** к создаваемым объектам
- **Фильтрация и сортировка** для платежей
- **Группа модераторов** с ограниченными правами доступа

---

## Структура проекта

Проект состоит из нескольких ключевых приложений:

*   **`config/`**: Основная конфигурация Django-проекта.
*   **`users/`**: Приложение для управления пользователями. Содержит кастомную модель `User`, платежи и систему прав доступа.
*   **`course/`**: Приложение для управления курсами учебного проекта. Содержит модели `Course` (Курс).
*   **`lesson/`**: Приложение для управления учебными материалами. Содержит модели `Lesson` (Урок).

---

## Модели данных

### 1. Пользователь (`users.User`)
Кастомная модель пользователя, унаследованная от `AbstractUser`.
*   **`email`**: Основное поле для авторизации (уникальное).
*   **`phone_number`**: Номер телефона (уникальный).
*   **`city`**: Город (необязательное).
*   **`avatar`**: Аватар пользователя (необязательное).
*   **`created_at`**: Дата создания аккаунта.

### 2. Курс (`course.Course`)
*   **`title`**: Название курса.
*   **`course_image`**: Изображение-превью для курса.
*   **`description`**: Описание курса.
*   **`owner`**: Владелец курса (связь с User).
*   **`created_at`**: Дата создания.

### 3. Урок (`lesson.Lesson`)
*   **`title`**: Название урока.
*   **`description`**: Описание урока.
*   **`preview`**: Изображение-превью для урока.
*   **`video_file`**: Ссылка на видео-материал.
*   **`course`**: Связь с моделью `Course` (ForeignKey). Один курс может содержать много уроков.
*   **`owner`**: Владелец урока (связь с User).
*   **`created_at`**: Дата создания.

### 4. Платеж (`users.Payment`)
*   **`user`**: Пользователь, совершивший платеж.
*   **`payment_date`**: Дата платежа.
*   **`paid_course`**: Оплаченный курс (может быть пустым).
*   **`paid_lesson`**: Оплаченный урок (может быть пустым).
*   **`amount`**: Сумма платежа.
*   **`payment_method`**: Способ оплаты (наличные/перевод).

---

## 🔐 Система прав доступа

### Роли пользователей:

#### 1. **Обычный пользователь**
- Может создавать, просматривать, редактировать и удалять ТОЛЬКО свои курсы и уроки
- Видит только свои платежи

#### 2. **Модератор** (группа "Модераторы")
- Может просматривать и редактировать ЛЮБЫЕ курсы и уроки
- НЕ может создавать новые курсы и уроки
- НЕ может удалять курсы и уроки
- Видит все платежи

#### 3. **Администратор** (is_staff=True)
- Полный доступ ко всем операциям
- Может управлять пользователями и группами

---

## 📡 API Endpoints (Эндпоинты)

### 🔐 Аутентификация (`/users/`)
*   `POST /users/register/` - Регистрация нового пользователя (доступно без авторизации)
*   `POST /users/login/` - Получение JWT токена (доступно без авторизации)
*   `POST /users/token/refresh/` - Обновление access токена
*   `GET /users/profile/` - Получение профиля текущего пользователя

### 👥 Пользователи (`/users/`)
*   `GET /users/users/` - Список пользователей (только для администраторов)
*   `GET /users/users/{id}/` - Профиль конкретного пользователя
*   `PUT /users/users/{id}/` - Обновление пользователя (владелец или администратор)
*   `DELETE /users/users/{id}/` - Удаление пользователя (владелец или администратор)

### 💰 Платежи (`/users/payments/`)
*   `GET /users/payments/` - Список платежей (с фильтрацией и сортировкой)
*   `POST /users/payments/` - Создание нового платежа
*   `GET /users/payments/{id}/` - Детали платежа

### 🎓 Курсы (`/course/`) - реализованы через `Viewsets`
*   `GET /course/` - Получение списка курсов (только свои для обычных пользователей, все для модераторов)
*   `POST /course/` - Создание нового курса (не доступно для модераторов)
*   `GET /course/{id}/` - Получение информации о конкретном курсе
*   `PUT /course/{id}/` - Полное обновление информации о курсе (владелец или модератор)
*   `PATCH /course/{id}/` - Частичное обновление информации о курсе (владелец или модератор)
*   `DELETE /course/{id}/` - Удаление курса (только владелец)

### 📚 Уроки (`/lesson/`) - реализованы через `Generic APIView`
*   `GET /lesson/` - Получение списка уроков (только свои для обычных пользователей, все для модераторов)
*   `POST /lesson/create/` - Создание нового урока (не доступно для модераторов)
*   `GET /lesson/{id}/` - Получение информации о конкретном уроке
*   `PUT /lesson/{id}/update/` - Обновление информации об уроке (владелец или модератор)
*   `DELETE /lesson/{id}/delete/` - Удаление урока (только владелец)

---

## 🛠 Установка и запуск проекта

### 1. Клонируйте репозиторий:

    
    git clone https://github.com/Klimchuk-sergei/HW_30_1.git
    cd HW_30_1

### 2.  Установка и запуск проекта

    2. Создайте и активируйте виртуальное окружение:
    
    python -m venv venv

    # Для Windows
    .\venv\Scripts\activate

    # Для macOS/Linux
    source venv/bin/activate

### 3.  **Установите зависимости:**
 
    pip install -r requirements.txt
 

### 4.  **Настройте переменные окружения:**
    *   Скопируйте файл `.env.example` в `.env`.
    *   Заполните `.env` вашими данными для подключения к базе данных PostgreSQL.
    
    Пример .env файла:
    DEBUG=True
    SECRET_KEY=your-secret-key-here
    DB_NAME=lms_db
    DB_USER=your_db_user
    DB_PASSWORD=your_db_password
    DB_HOST=localhost
    DB_PORT=5432

### 5.  **Примените миграции:**

    python manage.py makemigrations
    python manage.py migrate

### 6.  **Создайте группы и права доступа:**
    python manage.py create_groups

### 7.  **Создайте суперпользователя:**
    python manage.py createsuperuser

### 8.  **Запустите сервер для разработки:**
    python manage.py runserver

Проект будет доступен по адресу `http://127.0.0.1:8000/`.

---

## Тестирование API

### Примеры запросов через Postman:

### 1.  Регистрация пользователя
POST http://127.0.0.1:8000/users/register/
    Content-Type: application/json
```json
{
        "email": "test@example.com",
        "password": "securepassword123",
        "phone_number": "+79991234567",
        "city": "Москва"
    }
```
    
    
    

### 2.  **Авторизация и получение токена**
    POST http://127.0.0.1:8000/users/login/
    Content-Type: application/json
    
    {
        "email": "test@example.com",
        "password": "securepassword123"
    }

### 3.  **Создание курса (с JWT токеном)**
    POST http://127.0.0.1:8000/course/
    Authorization: Bearer ваш_jwt_токен
    Content-Type: application/json
    
    {
        "title": "Python для начинающих",
        "description": "Полный курс по основам Python"
    }

### 4.  **Фильтрация платежей**
    GET http://127.0.0.1:8000/users/payments/?paid_course=1&ordering=-payment_date
    Authorization: Bearer ваш_jwt_токен

### 👨‍💼 Управление группами пользователей
    Создание модератора через админ-панель:
    Перейдите в /admin/
    
    Войдите как суперпользователь
    
    В разделе "Authentication and Authorization" → "Groups" убедитесь, что есть группа "Модераторы"
    
    В разделе "Users" создайте или отредактируйте пользователя
    
    Добавьте пользователя в группу "Модераторы"

### Права группы "Модераторы":
    Просмотр любых курсов и уроков
    
    Редактирование любых курсов и уроков
    
    Запрет на создание и удаление курсов и уроков

### 📁 Структура прав доступа (permissions)
    Файл users/permissions.py содержит:
    
    IsModerator - проверка принадлежности к группе модераторов
    
    IsOwner - проверка владения объектом
    
    IsOwnerOrStaff - проверка владения или прав администратора